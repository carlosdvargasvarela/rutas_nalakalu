<% content_for :title, "Sin conexión" %>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6 text-center">
      <i class="bi bi-wifi-off display-1 text-muted mb-4"></i>
      <h1 class="mb-3">Sin conexión</h1>
      <p class="lead text-muted mb-4">
        No hay conexión a internet en este momento.
      </p>
      <p class="text-muted mb-4">
        Las acciones que realices se guardarán y se sincronizarán automáticamente cuando vuelvas a tener conexión.
      </p>
      <button onclick="window.location.reload()" class="btn btn-primary">
        <i class="bi bi-arrow-clockwise me-2"></i>
        Reintentar
      </button>
    </div>
  </div>
</div>