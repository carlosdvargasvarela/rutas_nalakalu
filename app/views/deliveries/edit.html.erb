<%# app/views/deliveries/edit.html.erb %>
<%= render "shared/flash_errors", objects: [@client, @order, @delivery, @address] %>
<div class="d-flex align-items-center mb-4">
  <i class="bi bi-truck me-2 fs-3 text-primary"></i>
  <h1 class="mb-0">Editar entrega</h1>
</div>
<div class="card shadow-sm">
  <div class="card-body">
    <%= render "deliveries/upsert_partials/form", delivery: @delivery, clients: [@client], addresses: @addresses %>
  </div>
</div>