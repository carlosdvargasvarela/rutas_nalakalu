<% if flash[:alert] %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
    <i class="bi bi-exclamation-triangle-fill me-2"></i>
    <%= flash[:alert] %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>

<div class="d-flex align-items-center mb-4">
  <i class="bi bi-box-seam fs-2 text-success me-3"></i>
  <div>
    <h1 class="h3 mb-0">Nuevo Mandado Interno</h1>
    <p class="text-muted mb-0 small">Registra entregas internas, traslados o tareas logísticas</p>
  </div>
</div>

<div class="card shadow-sm border-0">
  <div class="card-body">
    <%= render "deliveries/internal_delivery_partials/form_internal_delivery", delivery: @delivery %>
  </div>
</div>
