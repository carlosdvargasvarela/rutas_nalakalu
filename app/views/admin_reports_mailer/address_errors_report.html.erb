<div class="header" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);">
  <h1>⚠️ Informe Semanal de Errores en Direcciones</h1>
  <p>Semana del <%= l(@report_data[:week_start], format: :long) %> al <%= l(@report_data[:week_end], format: :long) %></p>
</div>
<div class="content">
  <div class="highlight-box" style="border-left-color: #dc3545;">
    <h2 style="margin-top: 0; color: #dc3545;">Resumen Ejecutivo</h2>
    <p>
      Durante la semana pasada se identificaron <strong><%= @report_data[:total_count] %></strong>
      entrega(s) con <strong>errores en sus direcciones</strong>, afectando a 
      <strong><%= @report_data[:affected_sellers_count] %></strong> vendedor(es).
    </p>
  </div>
  <div class="stats">
    <div class="stat-item">
      <span class="stat-number" style="color: #dc3545;"><%= @report_data[:total_count] %></span>
      <span class="stat-label">Entregas con errores</span>
    </div>
    <div class="stat-item">
      <span class="stat-number" style="color: #dc3545;"><%= @report_data[:affected_sellers_count] %></span>
      <span class="stat-label">Vendedores afectados</span>
    </div>
  </div>
  <h3 style="color: #dc3545; margin-top: 30px;">Tipos de Errores Detectados</h3>
  <table>
    <thead>
      <tr>
        <th>Tipo de Error</th>
        <th style="text-align: center;">Cantidad</th>
      </tr>
    </thead>
    <tbody>
      <% @report_data[:error_type_summary].each do |error_type, count| %>
        <tr>
          <td><%= error_type %></td>
          <td style="text-align: center;">
            <span class="error-badge"><%= count %></span>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <h3 style="color: #dc3545; margin-top: 30px;">Top 5 Vendedores con Más Errores</h3>
  <table>
    <thead>
      <tr>
        <th>Vendedor</th>
        <th>Código</th>
        <th style="text-align: center;">Errores</th>
        <th>Principales Tipos</th>
      </tr>
    </thead>
    <tbody>
      <% @report_data[:top_sellers].each do |seller| %>
        <tr>
          <td><strong><%= seller[:name] %></strong></td>
          <td><%= seller[:code] %></td>
          <td style="text-align: center;">
            <span class="error-badge"><%= seller[:count] %></span>
          </td>
          <td style="font-size: 12px;"><%= seller[:main_errors] %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <div class="attachment-notice">
    📎 <strong>Archivo Excel adjunto</strong><br>
    Se adjunta un archivo Excel con el detalle completo de todas las entregas con errores de dirección, 
    incluyendo coordenadas, tipo de error específico y responsable.
  </div>
  <p style="margin-top: 30px; color: #666; font-size: 14px;">
    <strong>Acción requerida:</strong> Coordinar con los vendedores para corregir estas direcciones 
    lo antes posible. Direcciones incorrectas generan retrasos, costos adicionales y afectan la experiencia del cliente.
  </p>
</div>
<div class="footer">
  <p>Este es un informe automático generado por el Sistema de Rutas NaLakalu</p>
  <p>Generado el <%= l(Time.current, format: :long) %></p>
</div>