<!DOCTYPE html>
<html>
  <head>
    <title>NaLakalu - Rutas de Entrega</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "chartkick", "Chart.bundle" %>
    <!-- Google Maps -->
    <script async defer src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_MAPS_API_KEY'] %>&libraries=places"></script>
  </head>
  <body>
    <!-- 🚩 Navbar ya no se destruye con Turbo -->
    <div id="navbar-container" data-turbo-permanent>
      <%= render "layouts/navbar" %>
    </div>
    <main class="container-fluid mt-4">
      <%= render "layouts/flashes" %>
      <%= yield %>
    </main>
    <!-- Modal global -->
    <div class="modal fade" id="noteModal" tabindex="-1" aria-hidden="true" data-controller="modal">
      <div class="modal-dialog">
        <div class="modal-content">
          <turbo-frame id="note_modal"></turbo-frame>
        </div>
      </div>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script>
      window.GOOGLE_MAPS_API_KEY = "<%= ENV['GOOGLE_MAPS_API_KEY'] %>";
    </script>
  </body>
</html>